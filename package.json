{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck:libs && pnpm --filter @workspace/rabab-legal run build && pnpm run prerender:public && pnpm run check:seo-output && pnpm --filter @workspace/api-server run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "lint": "pnpm -r --filter \"./artifacts/**\" --if-present run lint",
    "check:vercel-config": "node ./scripts/check-vercel-config.mjs",
    "prerender:public": "node ./scripts/prerender-public-pages.mjs",
    "check:seo-output": "node ./scripts/check-seo-output.mjs",
    "smoke:vercel-api": "node ./scripts/smoke-vercel-api.mjs"
  },
  "private": true,
  "dependencies": {
    "@types/express": "^5.0.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "prettier": "^3.9.5",
    "typescript": "~5.9.3"
  }
}
