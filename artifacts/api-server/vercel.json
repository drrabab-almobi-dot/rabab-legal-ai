{
  "version": 2,
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@10.34.5 --activate && pnpm install --frozen-lockfile --prod=false",
  "buildCommand": "cd ../.. && corepack enable && corepack prepare pnpm@10.34.5 --activate && pnpm --filter @workspace/api-server run build",
  "functions": {
    "api/index.mjs": {
      "runtime": "nodejs20.x"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.mjs"
    }
  ]
}