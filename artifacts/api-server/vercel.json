{
  "framework": "express",
  "installCommand": "(cd ../.. && corepack enable && corepack install && pnpm install --frozen-lockfile)",
  "buildCommand": "(cd ../.. && corepack install && pnpm --filter @workspace/api-server run build)",
  "outputDirectory": "dist"
}