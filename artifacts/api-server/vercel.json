{
  "framework": "express",
  "installCommand": "cd ../.. && corepack enable && corepack prepare --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && corepack prepare --activate && pnpm --filter @workspace/api-server run build",
  "outputDirectory": "dist"
}